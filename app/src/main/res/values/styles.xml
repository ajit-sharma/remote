<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="button_wrap">
        <item name="android:layout_marginBottom">@dimen/screen_padding</item>
        <item name="android:layout_marginTop">@dimen/screen_padding</item>
        <item name="android:layout_marginLeft">@dimen/screen_padding</item>
        <item name="android:layout_marginRight">@dimen/screen_padding</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>


    </style>

</resources>
